import localStorage_message from "./localStorage"

export let auth = {
    set_auth:function(){
   
        localStorage_message.saveLogin_auth({
            
        })
    },
    close_auth:function(){
           
    }
    
} 